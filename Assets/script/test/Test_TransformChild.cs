using UnityEngine;
using System.Collections;

public class Test_TransformChild : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //int numChild = 0;
        //foreach (Transform t in transform)
        //{
        //    numChild++;
        //}
        //Debug.Log("numchild = " + numChild);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
